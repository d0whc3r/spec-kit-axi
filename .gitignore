# Build artifacts
dist/
# Examples copied into the published site (single source of truth: /examples).
# Staged into web/public so Vite copies them into the build output. Synced by
# `pnpm examples:sync` locally and by the pages workflow on deploy.
web/public/examples/
# Axi review surface build output. `pnpm review:build` emits the compiled
# server + browser surface into templates/web-review/ (axi-server.mjs,
# axi-core.mjs, dist/). All build artifacts, shipped in the release zip.
/templates/web-review/
.wiki-staging/
upstream-catalog-issue-*.md
/.specify/
/specs/
# axi review surface runtime state (sessions, queued annotations)
.speckit-axi/
.agents/skills/speckit-*
.claude/skills/speckit-*
.github/agents/speckit.*.agent.md
.github/prompts/speckit.*.prompt.md

# Editor and OS metadata
.DS_Store
.vscode/
.idea/
*.swp
*.tmp

# Dependency caches (none expected, but defensive)
node_modules/

dist/

---
description: "TODO: describe what /speckit.axi does."
---

# Axi

TODO: define this command. Axi is a Spec Kit extension; this command file is the
canonical surface shipped in the release zip alongside `templates/`.

## User Input

```text
$ARGUMENTS
```

You **MUST** consider the user input before proceeding (if not empty).

## Output

TODO: describe what this command produces and where it writes.

Reference the shipped template at `templates/axi-template.md` when generating
output.

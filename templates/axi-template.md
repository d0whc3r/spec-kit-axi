# Axi Template

TODO: define the shipped template(s) for the axi extension. The lint pipeline
(`.github/scripts/lint-content.mjs`) requires the canonical headings below to be
present and in order; add real ones as the extension takes shape.

## Template

TODO: the template body that `/speckit.axi` fills in.
